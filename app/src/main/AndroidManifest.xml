<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/asset_1_0"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/asset_1_0_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mental_health_app"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BottleActivity"
            android:exported="true"/>
        <activity android:name=".DiaryActivity"
            android:exported="true"/>
        <activity android:name=".PlanActivity"
            android:exported="true"/>
        <activity android:name=".RewardActivity"
            android:exported="true"/>
        <activity android:name=".CryingGirlActivity"
            android:exported="true"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>